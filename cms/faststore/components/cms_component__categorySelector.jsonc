{
  "$extends": ["#/$defs/base-component"],
  "$componentKey": "CategorySelector",
  "$componentTitle": "Category Selector",
  "type": "object",
  "properties": {
    "selectedCategory": {
      "type": "string",
      "title": "Category",
      "description": "Dynamically loaded category from API",
      "ui:widget": "CategorySelector"
      // No "enum": filled dynamically in the React component
    }
  }
}